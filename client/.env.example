VITE_API_URL=http://localhost:3001/api

# Production only (e.g. on Render): point this at the deployed server before
# building, since Vite bakes env vars into the build at build time.
# VITE_API_URL=https://encartes-server.onrender.com/api
